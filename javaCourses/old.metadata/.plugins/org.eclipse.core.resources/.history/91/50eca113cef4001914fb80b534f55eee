package reservol;

import java.time.LocalDate;
import java.util.Scanner;

import dao.dao_vol;
import models.Avion;

public class Program {

	public static void start() {
		Display.Start();

		int userChoice = Utils.isUserInputOk(1);

		switch (userChoice) {
		case 1:
			menuVols();
			break;
		case 2:
			menuResas();
			break;
		case 3:
			Display.quitter();
			break;

		default:
			System.out.println("Error Program.java line22");
			break;
		}
	}

	/**
	 * 1: Création d’un vol 
	 * 2: Liste des vols 
	 * 3: Rechercher un avion par numéro 
	 * 4: Rechercher un avion par ville de départ et d’arrivée
	 */
	public static void menuVols() {
		Display.gestionVols();
		int userChoice = Utils.isUserInputOk(2);

		switch (userChoice) {
		case 1:
			System.out.println("Numéro de vol :\n");
//			dao_vol.createVol(numero_vol, avion, nombre_place, ville_depart, ville_arrive, date_depart)
			Scanner sc = new Scanner(System.in); 
			// Numero de vol doit avoir len == 4
			String numVol = "";
			do { 
				numVol = sc.nextLine();
				System.out.println("aa");
			} while (numVol.length() != 4);
			//String numero_vol,
			// Avion avion
			// int nombre_place
			// String ville_depart
			// String ville_arrive,
			// LocalDate date_depart
			break;
		case 2:
//			menuResas();
			break;
		case 3:
//			Display.quitter();
			break;
		case 4:
//			Display.quitter();
			break;

		}
	}

	public static void menuResas() {
		Display.gestionResa();

	}

}
