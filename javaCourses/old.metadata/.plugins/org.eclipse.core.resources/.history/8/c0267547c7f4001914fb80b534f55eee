package dao;

import models.Reservation;
import models.Vol;

public class dao_reservation {
	
	public static Reservation createReservation( Vol numero_vol, String nom_user, String prenom_user, int age_user, Long numero_resa ) {
		Reservation reservation = new Reservation(numero_vol, nom_user, prenom_user, age_user, numero_resa);
		return reservation;
	}

}
