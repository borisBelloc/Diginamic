import { DirectiveOneDirective } from './directive-one.directive';

describe('DirectiveOneDirective', () => {
  it('should create an instance', () => {
    const directive = new DirectiveOneDirective();
    expect(directive).toBeTruthy();
  });
});
